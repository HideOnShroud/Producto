export interface ItemInterface {
    sku: string,
    name: string,
    price: number,
    productType: string,
    size: number | null,
    weight: number | null,
    height: number | null,
    width: number | null,
    length: number | null
}